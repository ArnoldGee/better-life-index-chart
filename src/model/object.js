  store = {
  chartData: [ /* Data returned from papa CSV. Aquí s'ha de mirar lo de "songs"
                  Per recuperar la informació, la lògica ja la inclourem a dins de */],

  chartOptions: {
    selectedChart: '', /* el nom del chart seleccionat */
    options: [ /* aquí el que ens interessa és tenir una array de keys de totes les opcions possibles (excepte la primera, que és el país, que no ens interessa). Aquestes dades s'enxufen al drowpdown */],
  }
}

/** Falta crear els dos reducers! */